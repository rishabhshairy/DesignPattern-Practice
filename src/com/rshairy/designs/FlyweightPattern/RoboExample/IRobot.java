package com.rshairy.designs.FlyweightPattern.RoboExample;

/**
 * Designing this interface so, that
 * Extrinsic Data which in this case x and y co-ordinates are passed from external source
 */
public interface IRobot {
    void display(int x, int y);
}
